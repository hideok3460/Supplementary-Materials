# Supplementary File S3

`verification_canonical_noncanonical_CP_TNI_reproducibility_export.ipynb` is the executed exporter/validator associated with the single `ibm_phoenix` job `daj7e68mhr3c73e8kotg`.

It packages and validates the hardware provenance used by the manuscript, including per-circuit raw counts, ordered circuit metadata, final physical layouts, the backend calibration snapshot, manifest/provenance records, and integrity information. The resulting archive is supplied as Supplementary File S4.
